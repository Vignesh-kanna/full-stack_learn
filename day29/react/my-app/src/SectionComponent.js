import React from 'react'

const SectionComponent = () => {
  return (
    <div>
      
        <section className="App-section">
            <h2>About This App</h2>
            <p>This app is built using React. It demonstrates the use of components, props, and state management.</p>
            <p>React is a JavaScript library for building user interfaces. It allows developers to create large web applications that can change data, without reloading the page.</p>
            <p>Components are the building blocks of a React application. They can be reused and composed to create complex UIs.</p>
        </section>
    </div>
  )
}

export default SectionComponent
